export interface GeneralCountModel {
  visitedCount: number;
  downloadedCount: number;
}

export interface GeneralCountModelDate {
  date: string;
  visitedCount: number;
  downloadedCount: number;
}